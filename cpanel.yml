---
deployment:
  tasks:
    - export REPOPATH=/home/docegre1/repositories/doce-gregorini
    - export STGPATH=/home/docegre1/public_html 
    - /usr/bin/rsync -a --exclude=".git" --exclude=".cpanel.yml" $REPOPATH/ $STGPATH